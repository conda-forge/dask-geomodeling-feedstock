{% set name = "dask-geomodeling" %}
{% set version = "2.7.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 03283a4af9ce4de36ea8550f06db0eb417537d87680d695826a03073e6f3fe90

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - gdal >=3.4
    - dask-core >=2022
    - geopandas >=1
    - numpy >=1.23
    - pandas >=2
    - python >={{ python_min }}
    - pytz
    - scipy >=1.11
    - pyogrio
    - packaging

test:
  imports:
    - dask_geomodeling
    - dask_geomodeling.core
    - dask_geomodeling.geometry
    - dask_geomodeling.raster
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: http://github.com/nens/dask-geomodeling
  license: BSD-3-Clause
  license_file: LICENSE
  summary: On-the-fly operations on geographical maps
  description: |
    dsk-geomodeling is a collection of classes that are to be stacked together to
    create configurations for on-the-fly operations on geographical maps.
  doc_url: http://dask-geomodeling.readthedocs.io/
  dev_url: http://github.com/nens/dask-geomodeling

extra:
  recipe-maintainers:
    - caspervdw
    - reinout
